<header class="nav-bar">
  <nav>
    <div>
      <a id="logo" href="/"><strong>Bucket</strong>Listing</a>
    </div>
    <%if (user_id) {%>
      <div>
        <a href="/logout">Logout</a>
        <%if (user_id && index) {%>
          <a href=<%=`/todos/categories`%>>View ToDos</a>
          <%} else {%>
            <a href="/">Add ToDos</a>
            <%}%>
      </div>
      <%}%>
        <%if (!user_id) {%>
          <div>
            <a href="/register">Register</a>
            <!-- hardcoded to log in as user_id = 1 -->
            <a href="/login/1">Login</a>
          </div>
          <%}%>
  </nav>
</header>
